using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Leesoyi_chapter4 : MonoBehaviour
{
    // Start is called before the first frame update
    // 1. i++ : i+1 / ++i :출력 후 i+1
    // 2. int = 1;
    // Debug.Log(i = i +1); 답:2
    // Debug.Log(i++); 답:2
    // Debug.Log(++1); 답:4
    // Debug.Log(i += 1); 답:5
    // 3.int a = 10;
    // string b = a == 0 ? "가나다" : "ABC";
    // b : "ABC"
    
}
